# D-001 scope bounds
- TUI is already in `packages/ui/tui`; work is redesign + provider UX + launch alias, not a greenfield port from `dsh-TUI/`.
- First-class providers are the five named by the user. Remaining pi-ai catalog stays wizard/settings.
- Harness "optimization" is limited to TUI integration quality.
- Visual identity is a user decision; recommended original compact mark + Claude-Code-like palette.
